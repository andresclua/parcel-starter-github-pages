{
  "name": "your-project-name",
  "version": "1.0.0",
  "scripts": {
    "start": "parcel src/index.html -p 3003 --open",
    "build": "parcel build ./src/index.html",
    "deploy": "rm -rf dist/ && npm build && gh-pages -d dist"
  },
  "homepage": "https://andresclua.github.io/parcel-starter-github-pages",
  "dependencies": {
    "gh-pages": "^3.0.0"
  },
  "devDependencies": {
    "gh-pages": "^3.0.0",
    "parcel": "^2.8.3"
  }
}
